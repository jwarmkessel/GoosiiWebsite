extends ../layout
block content
	h1 All Daily Unique Participants


	- if(docs.length)
		table
			tr
				th Daily Unique Participants
				th
					each dup in docs
						tr
							td #{dup.id}
							td #{dup.company_id}
							td #{dup.timestamp}
							td #{dup.user_id}
							td
								a.btn(href="/dailyUniquePart/#{dup.id}/edit") Edit
							td
								form(method='post', action='/dailyUniquePart/' + dup.id)
									input(name='_method', value='DELETE', type='hidden')
									button.btn(type='submit') Delete
	- else
		p You don't have any dailyUniquePart!
	p
		a(href='/dailyUniquePart/new' class='btn primary') Add a Daily Unique Participant
